<!DOCTYPE html>
<html>
<head>
        <title>Windows on demand patch summary report - {{ HOSTS }} - {{ timestamp.stdout }}</title>

        <meta charset="UTF-8">
        <meta name="author" content="Jakub Wosko (a586404)">

        <style>
        /* Style the header */
        header {
                background-color: #666;
                padding: 5px;
                text-align: center;
                font-size: 35px;
                color: white;
        }

        article {
                padding: 20px;
        }
        div {
                        padding: 10px;
        }
        table {
                border-collapse: collapse;
                text-align: center;
                margin: 0px auto;
        }
        table, th, tr, td {
                border: 1px solid black;
        }
        td, th {
                padding: 5px;
        }
        tr:hover {background-color: #f5f5f5;}
        th {
                background-color: #4CAF50;
        }
        .info th {
                background-color: #F0F1E9;
        }
        .info {
                margin-bottom: 20px;
                font-size: 25px;
        }
        .legend {
                width: 570px;
                margin: auto;
                font-size: 12px
        }

        /* Style the footer */
        footer {
                background-color: #777;
                padding: 5px;
                text-align: left;
                color: white;
        }
        </style>
</head>
<body>
<header>
        <h1 align="center">Windows on demand patch summary report</h1>
</header>

<article>
        <div class="info">
                <table>
                        <tr>
                                <th>Execution Time</th>
                                <th>Customer</th>
                                <th>Machines Group Name</th>
                                <th>Number of machines</th>
                        </tr>
                        <tr>
                                <td>{{ timestamp.stdout }}</td>
                                <td>{{ customer }}</td>
                                <td>{{ HOSTS }}</td>
                                <td>{{ HOSTS_COUNT }}</td>
                        </tr>
                </table>
        </div>
        <div>
                <h2 align="center">WSUS repository status</h2>
                <table>
                        <tr>
                                <th>WSUS hostname</th>
                                <th>Last successful synchronization time</th>
                        </tr>
                        <tr>
                                <td>{{ groups.wsus[0] }}</td>
                                <td>{{ WsusSync.stdout | string }}</td>
                        </tr>
                </table>
        </div>
        <div>
                <h2 align="center">Patch summary</h2>
                <table>
                        <tr>
                                <th>Index</td>
                                <th>Host name</th>
                                <th>OS name</th>
                                <th>Available patches</th>
                                <th>Installed patches</th>
                                <th>Complete number of patches</th>
                        </tr>
                        {% for host in ansible_play_batch %}
                        <tr>
                                <td>{{ loop.index }}</td>
                                <td>{{ host }}</td>
                                <td>{{ hostvars[host]['ansible_facts'].distribution }}</td>
                                <td>{{ hostvars[host]['result'].found_update_count }}</td>
                                <td>{{ hostvars[host]['installed_updates_count'].stdout | int | default(0) }}</td>
                                {%     set fup = hostvars[host]['result'].found_update_count | int | default(0) %}
                                {%     set iup = hostvars[host]['installed_updates_count'].stdout | int | default(0) %}
                                <td>{{ fup + iup }}</td>
                        </tr>
                        {% endfor %}
                </table>
        </div>
        <div class="legend">
                <h3 align="center">Legend</h3>
                <p>
                   <b>Machines Group Name</b> - Ansible Tower group containing host names against which the patching has been executed<br>
                   <b>Available patches</b> - number of all security patches needed by the server, ready to install next patch cycle<br>
                   <b>Installed patches</b> - number of all patches installed on the server<br>
                   <b>Complete number of patches</b> - total number of applicable patches in the patch repository
                </p>
        </div>
</article>

<footer>
        This report was generated by Ansible Tower.
</footer>

</body>
</html>

